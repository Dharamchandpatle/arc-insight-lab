# arc-insight-lab

This repository contains the front-end for AI-NEXUS — an AI-powered interview analytics UI built with Vite, React, TypeScript and Tailwind.

Quick start (local):

```sh
git clone <YOUR_GIT_URL>
cd <YOUR_PROJECT_NAME>
npm install
npm run dev
```

Tech stack:
- Vite
- React + TypeScript
- Tailwind CSS

Deployment:
- Build for production: `npm run build` and serve `dist/` with any static host.

Notes:
- This repo previously included integrations with an external tool; those provider-specific references have been removed.
