import gsap from "gsap";
import React, { useEffect, useRef } from "react";

interface TranscriptionPanelProps {
  candidateText: string[];
  hrText: string[];
}

export { default } from "./interview/TranscriptionPanel";
